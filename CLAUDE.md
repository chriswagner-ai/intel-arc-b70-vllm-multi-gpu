# CLAUDE.md

This repo's instructions for AI assistants live in [`AGENTS.md`](AGENTS.md).
Read it first — it has the symptom → fix lookup table, the pinned baseline stack,
the two load-bearing fixes for multi-GPU, and the working conventions.

(`CLAUDE.md` exists so Claude Code auto-loads and is pointed here; `AGENTS.md` is
the portable entry point other agents read.)
